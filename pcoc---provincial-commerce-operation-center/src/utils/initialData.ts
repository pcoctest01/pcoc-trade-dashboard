import { Database } from '../types';

/**
 * ฐานข้อมูลเริ่มต้น (ค่าว่าง เพื่อรอเชื่อมต่อข้อมูลจริงจาก Google Sheets)
 */
export const INITIAL_BORDER_TRADE_DATA: Database = {};

export const INITIAL_DATABASE = INITIAL_BORDER_TRADE_DATA;
